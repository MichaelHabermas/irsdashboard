<!--
  Synthetic data only — no real taxpayer information.
  Placeholder directory for exported TensorFlow.js risk artifacts (Epic 3+).
-->

# `models/`

This folder exists so Docker build contexts and Compose volume mounts resolve deterministically.
Populate only with **synthetic / demo** model exports — never production taxpayer-derived weights.
