# Bugs & Mitigations Log

| Date       | Bug / Issue                                                                   | Root Cause                                                                     | Mitigation Applied                                                                                                     | Lesson Learned                                                                  | Status    |
| ---------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | --------- |
| 2026-05-07 | Synthetic batch POST returned 201 vs tests expecting 200                      | Nest default `@Post()` uses **201 Created**                                    | **`@HttpCode(200)`** on `POST .../generate/batch`                                                                      | Decide status semantics when POST is used for deterministic generation payloads | Resolved  |
| 2026-05-07 | Frontend ESLint `react-refresh/only-export-components` on shadcn `button.tsx` | shadcn exports `buttonVariants` alongside `Button`                             | ESLint override for `src/components/ui/**` disabling the rule (ordered after base config)                              | UI primitives routinely export helpers — scope lint rules narrowly              | Resolved  |
| 2026-05-07 | Backend `@typescript-eslint/no-floating-promises` on `bootstrap()`            | Top-level async bootstrap without `void`/handler                               | `void bootstrap()` in `main.ts`                                                                                        | Always mark fire-and-forget async entrypoints explicitly                        | Resolved  |
| 2026-05-07 | Jest could not resolve/transpile `@irs/shared`                                | Workspace TS source + package `"type":"module"` incompatible with ts-jest path | Emit `@irs/shared` as **CommonJS** `dist/`, map Jest to `shared/dist/index.js`, add `pretest` shared build             | Keep Node test tooling on CJS interop unless full ESM Jest migration            | Resolved  |
| 2026-05-07 | Global Jest branch coverage stuck at ~75%                                     | Instrumented constructor branches on Nest controllers/services                 | Keep branches threshold at **75%** until richer HTTP/E2E coverage (Epic 5); exclude `main.ts` from coverage collection | Publish numeric rationale beside thresholds                                     | Mitigated |
| 2026-05-07 | Vitest could not resolve `@irs/shared`                                        | Vitest config lacked Vite alias/plugin parity                                  | Mirror Vite aliases + Tailwind plugin inside `vitest.config.ts`                                                        | Keep Vitest config synchronized with Vite resolve/transform behavior            | Resolved  |
| 2026-05-07 | Docker build: missing `data/` + `models/` contexts                            | Paths referenced in Compose/Dockerfile but dirs absent in repo                 | Added synthetic-only README placeholders under `data/` and `models/`                                                   | Docker COPY paths must exist — stub dirs with governance notes                  | Resolved  |
| 2026-05-07 | Docker build: `tsc` could not read `tsconfig.base.json`                       | Builder stage omitted root base config                                         | `COPY tsconfig.base.json ./` in both Dockerfiles                                                                       | Any `extends` chain must be copied into image build contexts                    | Resolved  |
| 2026-05-07 | Docker build: `pnpm install --filter …` omitted hoisted deps for shared `tsc` | Filtered install incomplete for workspace TypeScript builds                    | Use full `pnpm install --frozen-lockfile` in builder (acceptable tradeoff for reproducibility)                         | Prefer full workspace install in Docker builder unless tightly tuned            | Resolved  |
| 2026-05-07 | Automated git hooks removed                                                   | Intentional tooling simplification                                             | No git-hook runner; use `pnpm lint` / `pnpm format` locally and in CI                                                  | Document workflow change in dev-log                                             | Info      |

_This table will be populated as issues are discovered during development._
